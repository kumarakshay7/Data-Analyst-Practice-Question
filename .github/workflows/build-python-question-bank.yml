name: Build Python Question Bank

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-python:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Manual rebuild notice
        run: echo 'Automatic Python page rebuilding is disabled so manual page fixes are not overwritten.'
